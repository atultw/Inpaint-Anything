# Minimal dependencies for LaMa inpainting only
# Core dependencies
torch>=1.8.0
torchvision>=0.9.0
numpy
pillow
pyyaml
omegaconf

# Image processing
opencv-python

# LaMa model dependencies (some versions pinned for compatibility)
easydict
scikit-image
kornia>=0.5.0
hydra-core>=1.1.0
pytorch-lightning>=1.2.9

# Additional utilities used by LaMa
albumentations>=0.5.2
tabulate
tqdm


